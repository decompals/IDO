#! smake
# 
# $Revision: 1.2 $

include /usr/include/make/commondefs

OBJECTS = glxhelper.o

CFILES = \
	glxhelper.c

OPTIMIZER =-g 
SHDLIBC =

default all: $(OBJECTS)

clean:
	rm -f $(OBJECTS)

clobber:
	rm -f $(OBJECTS) core Makedepend

targets:
